{
  "name": "PathWay Front (pathwaysaas)",
  "install": "npm ci && { [ -f .env.local ] || cp .env.example .env.local; }",
  "terminals": [
    {
      "name": "next-dev",
      "command": "npx next dev --hostname 0.0.0.0 --port 5500"
    }
  ],
  "ports": [{ "name": "web", "port": 5500 }]
}
